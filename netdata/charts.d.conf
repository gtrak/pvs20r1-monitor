# charts.d plugin configuration — reference content.
#
# install.sh applies these two lines to /etc/netdata/charts.d.conf idempotently
# (it won't clobber existing settings). Custom (non-stock) modules require =yes,
# NOT =force (force only works for built-in stock modules).
enable_all_charts="yes"
solar=yes
